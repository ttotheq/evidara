node_modules
next-env.d.ts
.next
dist
coverage
.turbo

# Env files are ignored by default; allowlist the two that are
# deliberately tracked (.env.test must never hold real secrets).
.env*
!.env.example
!.env.test

# Key material never belongs in the repo.
*.pem
*.key

*.log
*.tsbuildinfo
.DS_Store
test-results/
playwright-report/
